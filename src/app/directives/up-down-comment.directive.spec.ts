import { UpDownCommentDirective } from './up-down-comment.directive';

describe('UpDownCommentDirective', () => {
  it('should create an instance', () => {
    const directive = new UpDownCommentDirective();
    expect(directive).toBeTruthy();
  });
});
